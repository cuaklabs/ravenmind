import { type AddMessagesOptionsMetadata } from './AddMessagesOptionsMetadata.js';

export interface AddMessagesOptions {
  metadata?: AddMessagesOptionsMetadata | undefined;
  observationDate?: Date | undefined;
}
